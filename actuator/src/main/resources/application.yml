management:
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
#      show-components: always
  endpoints:
    web:
      exposure:
        include: "*"
      # exclude: env, beans # 제외할 actuator endpoint 제외 가능
